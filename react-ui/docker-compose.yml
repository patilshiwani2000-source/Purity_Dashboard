version: '3.8'

services:
  frontend:
    build:
      context: ./react-ui
      dockerfile: Dockerfile.frontend
    ports:
      - "3001:80"
    environment:
      - REACT_APP_API_URL=http://localhost:8000
    restart: unless-stopped